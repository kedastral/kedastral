apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-app
  namespace: multi-app-redis
  labels:
    app: api-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-app
  template:
    metadata:
      labels:
        app: api-app
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: api
          image: multi-app-api:latest
          imagePullPolicy: Never
          env:
            - name: PORT
              value: "8080"
            - name: APP_NAME
              value: "api"
          ports:
            - containerPort: 8080
              name: http
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: api-app
  namespace: multi-app-redis
  labels:
    app: api-app
spec:
  selector:
    app: api-app
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-app
  namespace: multi-app-redis
  labels:
    app: api-app
spec:
  selector:
    matchLabels:
      app: api-app
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
